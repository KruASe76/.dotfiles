[Unit]
Description=Daemon for sing-box proxy
After=network.target

[Service]
Type=exec
ExecStart=/usr/bin/fish -l -c 'exec sb-proxy %h/.sing-box-template.json'
Restart=always

[Install]
WantedBy=default.target
